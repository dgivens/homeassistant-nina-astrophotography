# 2.0 entity renames

Every entity whose `entity_id` or `unique_id` changed in 2.0, with the 1.4.5
name it replaces, plus the config entry, action, blueprint and card changes.
Appended by each phase-B and phase-C PR, and sorted by 1.4.5 entity id within
each platform in phase D.

The authoritative record is the entity registry snapshot (§8.6) — this file
must match it. It is `tests/ha/__snapshots__/test_snapshots.ambr`, with the
plain entity-id list beside it in `entity_ids.txt`; both are regenerated in
their own commit, because with ~90 rows the diff IS the review.

The snapshot is taken against the reference rig, so eleven ids named below are
absent from it: the ten dome entities, which need hardware nobody involved has,
and `sensor.<instance>_weather_cloud_cover`,
`sensor.<instance>_weather_sky_quality` and `sensor.<instance>_weather_star_fwhm`,
which this weather source reports as `"NaN"` and so never creates (§5.2.2).

Scope: **complete**. Every platform is migrated, and phase D adds the action,
blueprint and card changes at the end.

**An upgrade does not rename anything on its own.** Home Assistant keys the
registry on `unique_id`, and a row whose `unique_id` is unchanged keeps the
`entity_id` it was minted with, whatever device it moves to. The "2.0" column is
therefore what a **fresh** install generates; an existing install keeps its
1.4.5 `entity_id` (and its dashboards and automations keep working) until the
user renames the entity themselves.

`<instance>` is the instance name from the config flow, slugified — `N.I.N.A.`
by default, which slugifies to `n_i_n_a`. The 1.4.5 ids are what a 1.4.5 install
mints under Home Assistant 2026.9, which builds every entity id from area,
device and entity name — so the device name 1.4.5 hardcoded, `N.I.N.A.
Astrophotography`, is part of them. A user who installed 1.4.5 under an earlier
Home Assistant, before entity ids carried the device name, has the unprefixed
ids 1.4.5's README documents (`light.flat_panel_light`). Either way an upgrade
leaves them as they are.

The 2.0 ids likewise assume **no area is assigned when the entity is first
registered**: area is the first of Home Assistant's default id parts, so an
entity minted into an area carries the area name ahead of the device name.

## Entities

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `light.n_i_n_a_astrophotography_flat_panel_light` | `light.<instance>_flat_panel_light` | The panel is its own device, `"<instance> Flat Panel"` (§5.1), so the id is built from that device name plus the entity name `Light`. `unique_id` is **unchanged** at `{entry_id}_flat_panel_light`. |
| `switch.n_i_n_a_astrophotography_flat_panel_light` | removed | One entity per device function (§5.3.4): the panel's light was both a `light` and a `switch`; the `light` survives. Its `unique_id` was `{entry_id}_flat_light_switch`, which nothing in 2.0 claims, so an upgraded install keeps the row as an unavailable leftover until it is deleted. |

## Entities — `binary_sensor` (C1)

1.4.5 shipped twenty binary sensors; 2.0 ships fourteen. Seven of the twenty
survive and thirteen are removed. Twelve of those thirteen are on the design's
cut list; the thirteenth, `mount_slewing`, is a 1.4.5-only cut. The cut list's
remaining eight — `rotator_connected`, `switch_connected`,
`camera_dew_heater_on`, `dome_following`, `rotator_reversed`,
`flatdevice_cover_open`, `flatdevice_light_on`, `livestack_running` — exist only
on `wip/v2.0`, which never shipped, so they have nothing to rename from and get
no row.

**Every survivor keeps its 1.4.5 `unique_id`**, four of them through a
`unique_id_suffix` on the descriptor because the new `key` reads better than the
old one. An upgraded install therefore keeps its registry rows and its entity
ids, and an automation pointing at the 1.4.5 safety entity keeps working.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `binary_sensor.n_i_n_a_astrophotography_camera_connected` | removed | Availability (§5.2.1). A disconnected camera makes its entities `unavailable`, which is observable in automations. |
| `binary_sensor.n_i_n_a_astrophotography_camera_cooling` | removed | Read-only mirror (§5.2.3); `switch.<instance>_camera_cooler` (C3) is the survivor, and its state is the actual value rather than the last commanded one. |
| `binary_sensor.n_i_n_a_astrophotography_camera_exposing` | `binary_sensor.<instance>_camera_exposing` | `unique_id` **unchanged** at `_camera_exposing`, via `unique_id_suffix`. Moves to the `"<instance> Camera"` device; the `RUNNING` device class is dropped — exposing is a camera state, not a task. |
| `binary_sensor.n_i_n_a_astrophotography_dome_connected` | removed | Availability (§5.2.1). |
| `binary_sensor.n_i_n_a_astrophotography_dome_shutter_open` | removed | `sensor.<instance>_dome_shutter_status` (C6) — `ShutterStatus` has five values, and `== 0` threw four of them away. |
| `binary_sensor.n_i_n_a_astrophotography_filter_wheel_connected` | removed | Availability (§5.2.1). |
| `binary_sensor.n_i_n_a_astrophotography_flat_device_connected` | removed | Availability (§5.2.1). |
| `binary_sensor.n_i_n_a_astrophotography_focuser_connected` | removed | Availability (§5.2.1). |
| `binary_sensor.n_i_n_a_astrophotography_focuser_moving` | `binary_sensor.<instance>_focuser_moving` | `unique_id` **unchanged** at `_focuser_is_moving`. Now `DIAGNOSTIC` and **disabled by default** (long tail). |
| `binary_sensor.n_i_n_a_astrophotography_guider_active` | removed | Read-only mirror (§5.2.3) of `switch.<instance>_guider` (C3), which is on whenever the guider is running. `sensor.<instance>_guider_status` is retained alongside it: the switch cannot tell a settled lock from a lost one, and an automation that cares about the difference triggers on the sensor. |
| `binary_sensor.n_i_n_a_astrophotography_guider_connected` | removed | Availability (§5.2.1). |
| `binary_sensor.n_i_n_a_astrophotography_mount_at_home` | `binary_sensor.<instance>_mount_at_home` | `unique_id` **unchanged**. Moves to the `"<instance> Mount"` device. |
| `binary_sensor.n_i_n_a_astrophotography_mount_connected` | removed | Availability (§5.2.1). |
| `binary_sensor.n_i_n_a_astrophotography_mount_parked` | `binary_sensor.<instance>_mount_at_park` | `unique_id` **unchanged** at `_mount_parked`, via `unique_id_suffix`; the key and the translated name follow `MountModel.at_park` and the mount's own `AtPark`. |
| `binary_sensor.n_i_n_a_astrophotography_mount_slewing` | removed | `MountModel` carries no `Slewing`: it is closed to fields no entity, service or pure module consumes, and nothing above the seam can compute it. |
| `binary_sensor.n_i_n_a_astrophotography_mount_tracking` | removed | `select.<instance>_mount_tracking_rate` (C2). Tracking modes are per-mount, from `TrackingModes`. |
| `binary_sensor.n_i_n_a_astrophotography_observatory_safe` | `binary_sensor.<instance>_safety_monitor_unsafe` | `unique_id` **unchanged** at `_safetymonitor_is_safe`, via `unique_id_suffix`. **The polarity is unchanged too**: 1.4.5 already stored `not IsSafe`, so `on` meant UNSAFE then and means UNSAFE now — only the name stops contradicting it (§5.2.1). |
| `binary_sensor.n_i_n_a_astrophotography_safety_monitor_connected` | `binary_sensor.<instance>_safety_monitor_connected` | `unique_id` **unchanged** at `_safetymonitor_connected`, via `unique_id_suffix`. The one `*_connected` kept (§5.2.1): a roof-close automation must still fire when the monitor itself drops out. Now `DIAGNOSTIC`, and the only entity exempted from availability level 2. |
| `binary_sensor.n_i_n_a_astrophotography_sequence_running` | `binary_sensor.<instance>_sequence_running` | `unique_id` **unchanged**. A hub entity (`kind: None`) — it is rig-scoped, not equipment-scoped. Its **source changes**: §6.2's activity heuristic, never `/sequence/json` node `Status`, which persists from the loaded file and read `RUNNING` on a verified-idle rig. |
| `binary_sensor.n_i_n_a_astrophotography_weather_station_connected` | removed | Availability (§5.2.1). |
| — | `binary_sensor.<instance>_focuser_autofocus_failed` | **New** (§5.4). `on` for either way a run fails, which look nothing alike. A HUNG run is an absence — a start unanswered past the profile's `AutoFocusTimeoutSeconds` — and is fold-derived, so there is no timer to leak. A REJECTED run FINISHES normally and is invisible in the event stream: N.I.N.A. writes its report with no verdict on it, so the only evidence is `/equipment/focuser/last-af`'s R² under the profile's `RSquaredThreshold` (§4.4). The report outlives a restart, so it is believed only while it is newer than the session start. |
| — | `binary_sensor.<instance>_filter_wheel_moving` | New. `DIAGNOSTIC`, disabled by default. |
| — | `binary_sensor.<instance>_rotator_moving` | New. `DIAGNOSTIC`, disabled by default. |
| — | `binary_sensor.<instance>_rotator_synced` | New. Retained from `wip/v2.0` (§5.2.3): sky-PA `Position` is meaningful only when synced. `DIAGNOSTIC`, disabled by default. |
| — | `binary_sensor.<instance>_dome_at_park` | New. `DIAGNOSTIC`, disabled by default, `verified: False` (§5.3.1). |
| — | `binary_sensor.<instance>_dome_at_home` | New. `DIAGNOSTIC`, disabled by default, `verified: False`. |
| — | `binary_sensor.<instance>_dome_slewing` | New. `DIAGNOSTIC`, disabled by default, `verified: False`. |

## Entities — `sensor`, the session family (C7)

1.4.5 shipped the session and last-image statistics **twice**: a polled family
in `sensor.py` read off `/image-history`, and a pushed family in
`frame_stats_sensor.py` fed by `IMAGE-SAVE`. They disagreed, and the pushed
semantics won by measurement (§5.2.4) — after a dawn flat run the polled
`Last Image HFR` read `0` and `Last Image Mean ADU` read `33139.77`, which is
exactly the last FLAT's mean. **So the survivor keeps the PUSHED entity's
`unique_id`** (`frame_*`) and the polled duplicate is the row that goes.

Twenty-four 1.4.5 sensors collapse to thirteen hub entities. Every one of the
thirteen is a survivor: none is new, and none changes its `unique_id`.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `sensor.n_i_n_a_astrophotography_frame_sparkline_data` | removed | `_frame_sparkline_data` — four series in the attributes of a sensor whose state was the frame count. Phase D's card reads recorder history. |
| `sensor.n_i_n_a_astrophotography_frames_per_filter` | removed | `_frame_per_filter_counts` — a sensor whose state was the frame count and whose payload was the real content. It is now `by_filter` on `session_avg_hfr`. |
| `sensor.n_i_n_a_astrophotography_hfr_trend` | removed | Presentation (`_frame_hfr_trend`, `_frame_hfr_trend_delta`): a two-window comparison a template or a card computes from `session_avg_hfr`'s history. |
| `sensor.n_i_n_a_astrophotography_last_frame_exposure` | `sensor.<instance>_last_image_exposure` | `unique_id` **unchanged** at `_frame_last_exposure`. |
| `sensor.n_i_n_a_astrophotography_last_frame_filter` | `sensor.<instance>_last_image_filter` | `unique_id` **unchanged** at `_frame_last_filter`. |
| `sensor.n_i_n_a_astrophotography_last_frame_guide_rms` | `sensor.<instance>_last_image_rms` | `unique_id` **unchanged** at `_frame_last_rms`. Arcseconds, so it is comparable with `sensor.<instance>_guider_rms_total` and across rigs; a total of 0 is no guiding rather than perfect guiding, and is `unknown`. |
| `sensor.n_i_n_a_astrophotography_last_frame_hfr` | `sensor.<instance>_last_image_hfr` | `unique_id` **unchanged** at `_frame_last_hfr`. Reads the newest LIGHT, never the newest frame. |
| `sensor.n_i_n_a_astrophotography_last_frame_mean_adu` | `sensor.<instance>_last_image_mean_adu` | `unique_id` **unchanged** at `_frame_last_mean_adu`. |
| `sensor.n_i_n_a_astrophotography_last_frame_min_adu` | removed | `Frame` carries no `Min`: `api/models.py` is closed to fields nothing above the seam consumes. Same for `_frame_last_max_adu`, `_frame_last_median_adu`, `_frame_last_std_dev_adu` and `_frame_last_hfr_std_dev`. |
| `sensor.n_i_n_a_astrophotography_last_frame_stars` | `sensor.<instance>_last_image_star_count` | `unique_id` **unchanged** at `_frame_last_stars`. |
| `sensor.n_i_n_a_astrophotography_last_frame_target` | `sensor.<instance>_last_image_target` | `unique_id` **unchanged** at `_frame_last_target`. |
| `sensor.n_i_n_a_astrophotography_last_image_hfr` | removed | The POLLED duplicate (§5.2.4). It read `0` after a dawn flat run, because the calibration HFR sentinel of zero looks like a measurement. Its `unique_id` was `_image_last_hfr`, which nothing in 2.0 claims. |
| `sensor.n_i_n_a_astrophotography_last_image_mean_adu` | removed | The polled duplicate; `_image_last_mean_adu`. It read the last FLAT's 33,139.77. |
| `sensor.n_i_n_a_astrophotography_last_image_star_count` | removed | The polled duplicate; `_image_last_star_count`. It read `unknown`, the `-1` sentinel being caught where the HFR `0` was not. |
| `sensor.n_i_n_a_astrophotography_rolling_avg_hfr_10` | removed | Presentation, not state (`_frame_rolling_avg_hfr`). A rolling window over the last ten frames is a card's job, and Home Assistant's own statistics helpers do it over any window. Same for `_frame_rolling_avg_stars` and `_frame_rolling_avg_adu`. |
| `sensor.n_i_n_a_astrophotography_session_avg_hfr` | `sensor.<instance>_session_avg_hfr` | `unique_id` **unchanged** at `_frame_session_avg_hfr`. Now carries the `by_target` and `by_filter` breakdowns as attributes (§4.4): session-wide means are dominated by whichever target got the most frames, and per-target means ranged 1.429–1.667 against a session-wide 1.513. |
| `sensor.n_i_n_a_astrophotography_session_avg_stars` | `sensor.<instance>_session_avg_stars` | `unique_id` **unchanged** at `_frame_session_avg_stars`. |
| `sensor.n_i_n_a_astrophotography_session_best_hfr` | `sensor.<instance>_session_best_hfr` | `unique_id` **unchanged** at `_frame_session_min_hfr`. |
| `sensor.n_i_n_a_astrophotography_session_frame_count` | `sensor.<instance>_session_image_count` | `unique_id` **unchanged** at `_frame_session_count`. Counts every frame in the session window, calibration included — "did the flats run?" is a question the lights-only aggregates cannot answer. `light_count` rides along as an attribute. **`state_class` changes** from `total_increasing` to `measurement`: the count resets at the rollover, and a sum across sessions is not what the entity means. |
| `sensor.n_i_n_a_astrophotography_session_image_count` | removed | The polled duplicate; `_image_count`. It counted the whole `/image-history` payload, which is process-scoped, not the night. |
| `sensor.n_i_n_a_astrophotography_session_integration_time` | `sensor.<instance>_session_integration_time` | `unique_id` **unchanged** at `_frame_session_integration`. **Breaking for statistics:** the unit moves from `min` to `h` and the `state_class` from `total_increasing` to `measurement`, with `device_class: duration` added. Both are HA duration units so the recorder converts, but the statistics type change means the long-term series restarts. |
| `sensor.n_i_n_a_astrophotography_session_worst_hfr` | `sensor.<instance>_session_worst_hfr` | `unique_id` **unchanged** at `_frame_session_max_hfr`. No longer `DIAGNOSTIC` — the three HFR aggregates belong together. |
| — | `sensor.<instance>_session_start` | **New** (§5.4). The most recent rollover in the RIG's local time: 12:00 UTC is 07:00 on a UTC−5 rig, inside its dawn flats. `device_class: timestamp`. |

## Entities — `sensor`, the weather channels (C7)

Thirteen ObservingConditions channels, each keeping its 1.4.5 `unique_id`. What
changes is that they are **created on sight and kept** (§5.2.2) rather than
registered unconditionally, and that they move from the hub to the
`"<instance> Weather"` device — so a **fresh** install's ids carry `weather`
twice over (`sensor.n_i_n_a_weather_sky_brightness`), while an upgraded install
keeps the 1.4.5 ids.

An upgraded install has all thirteen rows in its registry already, so all
thirteen come back at setup, and the ones the active source cannot report read
`unavailable` rather than `unknown`. That is the intended behaviour, not a
regression: 1.4.5 left them at `unknown` forever.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `sensor.n_i_n_a_astrophotography_atmospheric_seeing` | `sensor.<instance>_weather_star_fwhm` | `unique_id` **unchanged** at `_weather_seeing`. Renamed to the wire's own `StarFWHM`: "seeing" is a claim about the atmosphere, and this is one station's FWHM measurement. |
| `sensor.n_i_n_a_astrophotography_barometric_pressure` | `sensor.<instance>_weather_pressure` | `unique_id` **unchanged** at `_weather_pressure`. `device_class` moves from `pressure` to `atmospheric_pressure`, which is what an ObservingConditions station reports. |
| `sensor.n_i_n_a_astrophotography_cloud_cover` | `sensor.<instance>_weather_cloud_cover` | `unique_id` **unchanged** at `_weather_cloud_cover`. No device class: Home Assistant has none for cloud cover. |
| `sensor.n_i_n_a_astrophotography_dew_point` | `sensor.<instance>_weather_dew_point` | `unique_id` **unchanged** at `_weather_dew_point`. |
| `sensor.n_i_n_a_astrophotography_rain_rate` | `sensor.<instance>_weather_rain_rate` | `unique_id` **unchanged** at `_weather_rain_rate`. Gains `device_class: precipitation_intensity`. |
| `sensor.n_i_n_a_astrophotography_sky_brightness` | `sensor.<instance>_weather_sky_brightness` | `unique_id` **unchanged** at `_weather_sky_brightness`. LUX with `device_class: illuminance` — `SkyBrightness` and `SkyQuality` are two distinct ASCOM properties, and the rig reports `SkyBrightness 5692` beside `SkyQuality "NaN"`. |
| `sensor.n_i_n_a_astrophotography_sky_quality` | `sensor.<instance>_weather_sky_quality` | `unique_id` **unchanged** at `_weather_sky_quality`. `mag/arcsec²`, no device class — Home Assistant has none. Neither captured source reports it, so it appears only on a rig that has an SQM. |
| `sensor.n_i_n_a_astrophotography_sky_temperature` | `sensor.<instance>_weather_sky_temperature` | `unique_id` **unchanged** at `_weather_sky_temperature`. |
| `sensor.n_i_n_a_astrophotography_weather_humidity` | `sensor.<instance>_weather_humidity` | `unique_id` **unchanged**. |
| `sensor.n_i_n_a_astrophotography_weather_station_name` | `sensor.<instance>_weather_source` | `unique_id` **unchanged** at `_weather_name`. §5.4 lists `sensor.weather_source` as an addition, but it reports exactly what 1.4.5's station-name sensor reported, so it is a rename and the row survives. On the **hub**, not the weather device: it says which source is feeding the channels, and has to stay readable across a source swap. Falls back to the opaque `DeviceId` for a driver that reports no name. |
| `sensor.n_i_n_a_astrophotography_weather_temperature` | `sensor.<instance>_weather_temperature` | `unique_id` **unchanged** at `_weather_temperature`. |
| `sensor.n_i_n_a_astrophotography_wind_direction` | `sensor.<instance>_weather_wind_direction` | `unique_id` **unchanged** at `_weather_wind_direction`. Gains `device_class: wind_direction` with `state_class: measurement_angle`: averaging a compass bearing the ordinary way puts north between east and west. |
| `sensor.n_i_n_a_astrophotography_wind_gust` | `sensor.<instance>_weather_wind_gust` | `unique_id` **unchanged** at `_weather_wind_gust`. |
| `sensor.n_i_n_a_astrophotography_wind_speed` | `sensor.<instance>_weather_wind_speed` | `unique_id` **unchanged** at `_weather_wind_speed`. |

## Entities — `number` and `select` (C2)

1.4.5 shipped seven numbers and two selects. 2.0 ships seven numbers — three of
the 1.4.5 seven, plus four new ones — and the same two selects. **Every survivor
keeps its 1.4.5 `unique_id`**, through a `unique_id_suffix` on the descriptor,
so an upgraded install keeps its registry rows and the automations on them.

The four numbers with no 1.4.5 row — `flat_panel_brightness`,
`camera_usb_limit`, `rotator_mechanical_position` and `dome_azimuth` — are
§5.2.3's survivors of read-only sensors that existed only on `wip/v2.0`, so they
have nothing to rename from. `dome_azimuth` appears only on a rig with a dome
(§5.2.2), and `camera_usb_limit` and `rotator_mechanical_position` ship
`DIAGNOSTIC` and disabled.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `number.n_i_n_a_astrophotography_camera_binning` | removed | Binning is a mode name (`"1x1"`), not a number: a slider of 1–4 cannot express the `BinningModes` the camera reports, and `set-binning` takes `x` and `y`. |
| `number.n_i_n_a_astrophotography_camera_cooling_setpoint` | `number.<instance>_camera_target_temperature` | `unique_id` **unchanged** at `_camera_cooling_setpoint`, via `unique_id_suffix`. Named for `CameraInfo.TargetTemp`, which is what it reads. There is no setpoint endpoint: changing it is a cool-down to the new value. |
| `number.n_i_n_a_astrophotography_camera_gain` | removed | No client method binds gain: `/equipment/camera/set-gain` is not in the 2.0 client, and `models.py` is closed to fields nothing consumes. 1.4.5 reached through `client._get` to send it. |
| `number.n_i_n_a_astrophotography_camera_offset` | removed | As above, for `/equipment/camera/set-offset`. |
| `number.n_i_n_a_astrophotography_filter_wheel_slot` | removed | `select.<instance>_filter_wheel_filter` is the survivor: the wheel reports filter NAMES, and a slot number is the same choice with the names thrown away. |
| `number.n_i_n_a_astrophotography_focuser_target_position` | `number.<instance>_focuser_position` | `unique_id` **unchanged** at `_focuser_position_control`. Not a *target*: the state is the focuser's ACTUAL position (§5.2.3), so the name no longer claims otherwise. `sensor.<instance>_focuser_position` is reinstated alongside it (C6) for long-term statistics. |
| `number.n_i_n_a_astrophotography_rotator_position` | `number.<instance>_rotator_position` | `unique_id` **unchanged** at `_rotator_position_control`. Sky position angle, meaningful only while `binary_sensor.<instance>_rotator_synced` is on. |
| `select.n_i_n_a_astrophotography_active_filter` | `select.<instance>_filter_wheel_filter` | `unique_id` **unchanged** at `_filterwheel_select`. Options are the wheel's own `AvailableFilters`; 1.4.5's `"—"` placeholder for an empty wheel is gone, since an empty option list is what "no filters" means. `filterId` is the wheel's own slot `Id`, not the option's position — every captured wheel numbers from zero in list order, so the two agree there, but a wheel is free not to and a wrong slot changes to the wrong filter while answering `Success: true`. |
| `select.n_i_n_a_astrophotography_mount_tracking_rate` | `select.<instance>_mount_tracking_rate` | `unique_id` **unchanged** at `_tracking_rate_select`. Options now come from the mount's `TrackingModes` rather than the full five-value enum — this mount offers four and no King — and the sent `mode` is the API's enum value rather than the position in that list (§5.3.6). 1.4.5 also read a `TrackingRate` field the wire does not send and defaulted to `Sidereal`; the state is `TrackingMode`, and `unknown` when the mount reports none. |

## Entities — `switch` (C3)

1.4.5 shipped four switches. 2.0 ships seven, plus one per **binary** channel of
the N.I.N.A. switch device: two of the 1.4.5 four survive, one is superseded by
a `select`, and one was already recorded above as removed in phase A.

The new switches with no 1.4.5 row — `camera_dew_heater`, `flat_panel_cover`,
`rotator_reverse`, `dome_following` and the switch-device channels — are
§5.2.3's survivors of read-only binary sensors, so they have nothing to rename
from; `switch.<instance>_livestack` is likewise new here, since 1.4.5 had no
livestack support at all. `rotator_reverse` and `dome_following` ship
`DIAGNOSTIC` and disabled, `dome_following` appears only on a rig with a dome
(§5.2.2), and `flat_panel_cover` only on a panel reporting
`SupportsOpenClose`.

The channel entities are `switch.<instance>_switch_<channel name>` with
`unique_id` `{entry_id}_switch_channel_{Id}` — keyed on the channel's own `Id`,
so a channel added to the driver later does not renumber the existing ones.
Non-binary channels are not on this platform (C8 places them).

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `switch.n_i_n_a_astrophotography_autoguiding` | `switch.<instance>_guider` | `unique_id` **unchanged** at `_guider_switch`. On whenever the guider is RUNNING — every `State` but `Stopped` — where 1.4.5 read `State == "Guiding"` and so showed off through `LostLock` and `Calibrating`. Named for the device it is the one function of, so it takes the device's own name. |
| `switch.n_i_n_a_astrophotography_camera_cooler` | `switch.<instance>_camera_cooler` | `unique_id` **unchanged** at `_camera_cooler_switch`. The setpoint is now the camera's own `TargetTemp` rather than a hardcoded −10 °C, and the ramp is left to the profile rather than a hardcoded 15 minutes; a camera reporting no setpoint is refused instead of cooled to a guess. |
| `switch.n_i_n_a_astrophotography_flat_panel_light` | removed | Already recorded in phase A above (§5.3.4): the `light` survives. |
| `switch.n_i_n_a_astrophotography_mount_tracking` | removed | `select.<instance>_mount_tracking_rate` (C2) is the survivor: `Stopped` is one of the mount's own tracking modes, so the switch and the select were the same control with the switch throwing the rates away. |

## Entities — `button` (C4)

1.4.5 shipped twelve buttons, all of them on the hub device. 2.0 ships twelve
too: ten of the 1.4.5 twelve survive, two are removed, and two are new. **Every
survivor keeps its 1.4.5 `unique_id`**, through a `unique_id_suffix` on the
descriptor — the 1.4.5 keys were all `btn_`-prefixed and the new keys are not —
so an upgraded install keeps its registry rows and its entity ids.

What changes for all ten is the **device**: a button now hangs off the equipment
it commands (§5.1), so a fresh install's ids carry that device name, and the two
sequence buttons stay on the hub because sequence control is rig-scoped. The
four dome buttons are also **created on sight** (§5.2.2) rather than
unconditionally, and ship `DIAGNOSTIC`, disabled by default and `verified:
False` (§5.3.1) — so an install upgraded on a rig with no dome keeps
`btn_dome_open`, `btn_dome_close` and `btn_dome_park` as unavailable leftover
rows until they are deleted.

**A press awaits the HTTP round trip and nothing more.** 1.4.5 did the same, and
it stays that way deliberately: v2 issues no request id, so the completion event
for a five-minute autofocus cannot be attributed to the caller that started it.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `button.n_i_n_a_astrophotography_abort_capture` | `button.<instance>_camera_abort_exposure` | `unique_id` **unchanged** at `_btn_camera_abort`. Named for the endpoint it sends, `/equipment/camera/abort-exposure`; it aborts the exposure in progress, not a sequence. |
| `button.n_i_n_a_astrophotography_close_dome` | `button.<instance>_dome_close` | `unique_id` **unchanged** at `_btn_dome_close`. As above. |
| `button.n_i_n_a_astrophotography_mount_find_home` | `button.<instance>_mount_find_home` | `unique_id` **unchanged** at `_btn_mount_find_home`. |
| `button.n_i_n_a_astrophotography_open_dome` | `button.<instance>_dome_open` | `unique_id` **unchanged** at `_btn_dome_open`. Now `DIAGNOSTIC`, disabled by default, `verified: False`, and created only on a rig that reports a dome. |
| `button.n_i_n_a_astrophotography_park_dome` | `button.<instance>_dome_park` | `unique_id` **unchanged** at `_btn_dome_park`. As above. |
| `button.n_i_n_a_astrophotography_park_mount` | `button.<instance>_mount_park` | `unique_id` **unchanged** at `_btn_mount_park`. Moves to the `"<instance> Mount"` device. |
| `button.n_i_n_a_astrophotography_run_auto_focus` | `button.<instance>_focuser_auto_focus` | `unique_id` **unchanged** at `_btn_auto_focus`. Moves to the `"<instance> Focuser"` device. Whether the run failed is `binary_sensor.<instance>_focuser_autofocus_failed` (C1), not this button. |
| `button.n_i_n_a_astrophotography_start_guiding` | removed | `switch.<instance>_guider` (C3) is the survivor: guiding is a state that can be read back, and a pair of buttons cannot report it. Its `unique_id` was `{entry_id}_btn_guider_start`, which nothing in 2.0 claims. |
| `button.n_i_n_a_astrophotography_start_sequence` | `button.<instance>_sequence_start` | `unique_id` **unchanged** at `_btn_sequence_start`. Stays on the hub: sequence control is rig-scoped. |
| `button.n_i_n_a_astrophotography_stop_guiding` | removed | As above; `_btn_guider_stop`. |
| `button.n_i_n_a_astrophotography_stop_sequence` | `button.<instance>_sequence_stop` | `unique_id` **unchanged** at `_btn_sequence_stop`. |
| `button.n_i_n_a_astrophotography_unpark_mount` | `button.<instance>_mount_unpark` | `unique_id` **unchanged** at `_btn_mount_unpark`. |
| — | `button.<instance>_dome_home` | **New.** `/equipment/dome/home` had no 1.4.5 button. `DIAGNOSTIC`, disabled by default, `verified: False`. |
| — | `button.<instance>_guider_clear_calibration` | **New.** `DIAGNOSTIC`. One of the seven handlers that answer `Success: false, Error: "", StatusCode: 200` on a call that worked (§3.5) — which the client classifies as success, so the press returns normally. |

## Entities — `image` (C5)

1.4.5 shipped one image entity; 2.0 ships two.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `image.n_i_n_a_astrophotography_latest_captured_frame` | `image.<instance>_last_frame` | `unique_id` **unchanged** at `{entry_id}_latest_image`. Stays on the hub: `/image/0` indexes the rig's own history, so a camera disconnecting must not take it down. Its timestamp is now the newest frame in the fold rather than the moment the integration loaded. |
| — | `image.<instance>_livestack` | **New** (§5.3.2). The accumulating stack from `/livestack/image/{target}/{filter}`, and a better dashboard image than the last raw sub. Created only once a `STACK-UPDATED` has named a target and filter — that event says which pair is currently accumulating, where `/livestack/image/available` lists every pair the plugin holds without saying which is current. |

## Entities — `sensor`, the equipment readings and the sequence (C6)

1.4.5 shipped thirteen equipment sensors plus three sequence sensors. 2.0 ships
twenty-one equipment sensors and two sequence ones: nine of the thirteen
survive, four are cut, twelve are new, and one of the three sequence sensors is
cut. **Every survivor keeps its 1.4.5 `unique_id`**, four of them through a
`unique_id_suffix` because the new `key` reads better than the old one.

The twelve with no 1.4.5 row — `mount_side_of_pier`, `flat_panel_cover_state`,
`dome_shutter_status`, and the guider, rotator and dome entities that existed
only on `wip/v2.0` — have nothing to rename from. The long tail ships
`DIAGNOSTIC`; `focuser_position`, `focuser_step_size` and `dome_shutter_status`
also ship disabled by default.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `sensor.n_i_n_a_astrophotography_camera_cooler_power` | `sensor.<instance>_camera_cooler_power` | `unique_id` **unchanged**. Transiently `"NaN"` on a warm camera, so it is deliberately NOT under §5.2.2's create-on-sight rule: a rig configured by day must keep the entity. |
| `sensor.n_i_n_a_astrophotography_camera_gain` | `sensor.<instance>_camera_gain` | `unique_id` **unchanged**. Now `DIAGNOSTIC`. |
| `sensor.n_i_n_a_astrophotography_camera_name` | removed | §5.1 moved driver metadata onto the device registry: the name is the device's, and `DriverVersion` its `sw_version`. |
| `sensor.n_i_n_a_astrophotography_camera_offset` | `sensor.<instance>_camera_offset` | `unique_id` **unchanged**. Now `DIAGNOSTIC`. |
| `sensor.n_i_n_a_astrophotography_camera_status` | `sensor.<instance>_camera_state` | `unique_id` **unchanged** at `_camera_status`. Named for `CameraInfo.CameraState`, which is what it reads. Retained beside `binary_sensor.<instance>_camera_exposing` for the same reason `guider_status` is retained beside `switch.<instance>_guider` (§5.2.3): the flag answers "is it exposing", and `Downloading`, `Waiting` and `Error` are the states an automation about a stalled camera needs. Now `DIAGNOSTIC`. |
| `sensor.n_i_n_a_astrophotography_camera_target_temperature` | removed | `number.<instance>_camera_target_temperature` (C2) is the survivor, and its state is the actual `TargetTemp` (§5.2.3). |
| `sensor.n_i_n_a_astrophotography_camera_temperature` | `sensor.<instance>_camera_temperature` | `unique_id` **unchanged**. Moves to the `"<instance> Camera"` device, which is where the `camera_` prefix in the entity id now comes from. |
| `sensor.n_i_n_a_astrophotography_current_filter` | removed | `select.<instance>_filter_wheel_filter` (C2) is the survivor: its state is the wheel's actual `SelectedFilter`, so the sensor was the same reading with the control thrown away — the same rule that cut `sensor.mount_tracking_mode`. |
| `sensor.n_i_n_a_astrophotography_focuser_position` | `sensor.<instance>_focuser_position` | `unique_id` **unchanged**. Now **disabled by default**, and reinstated deliberately beside `number.<instance>_focuser_position` (§5.2.3): `NumberEntity` carries no `state_class`, and position against temperature is the standard temp-comp-slope diagnostic — which this rig needs, reporting `TempCompAvailable: false`. |
| `sensor.n_i_n_a_astrophotography_focuser_step_size` | `sensor.<instance>_focuser_step_size` | `unique_id` **unchanged**. `DIAGNOSTIC` and now disabled by default, and its `state_class` is gone: it is a driver constant, and a statistic over an unchanging number is noise. |
| `sensor.n_i_n_a_astrophotography_focuser_temperature` | `sensor.<instance>_focuser_temperature` | `unique_id` **unchanged**. |
| `sensor.n_i_n_a_astrophotography_guider_rms_dec` | `sensor.<instance>_guider_rms_declination` | `unique_id` **unchanged**. Arcseconds, and now `DIAGNOSTIC`. |
| `sensor.n_i_n_a_astrophotography_guider_rms_ra` | `sensor.<instance>_guider_rms_ra` | `unique_id` **unchanged**. Arcseconds, and now `DIAGNOSTIC`. |
| `sensor.n_i_n_a_astrophotography_guider_rms_total` | `sensor.<instance>_guider_rms_total` | `unique_id` **unchanged**. Now ARCSECONDS rather than the driver's pixels, so it is comparable across rigs and with `sensor.<instance>_last_image_rms`. |
| `sensor.n_i_n_a_astrophotography_guider_status` | `sensor.<instance>_guider_status` | `unique_id` **unchanged**. Retained (§5.2.3): `switch.<instance>_guider` is on for every `State` but `Stopped`, so it cannot tell a lost lock from a settled one. |
| `sensor.n_i_n_a_astrophotography_mount_altitude` | `sensor.<instance>_mount_altitude` | `unique_id` **unchanged**. |
| `sensor.n_i_n_a_astrophotography_mount_azimuth` | `sensor.<instance>_mount_azimuth` | `unique_id` **unchanged**. |
| `sensor.n_i_n_a_astrophotography_mount_dec` | `sensor.<instance>_mount_declination` | `unique_id` **unchanged** at `_mount_dec`. |
| `sensor.n_i_n_a_astrophotography_mount_name` | removed | Device metadata, as `camera_name` above. Its `unique_id` was `_mount_status`. |
| `sensor.n_i_n_a_astrophotography_mount_ra` | `sensor.<instance>_mount_right_ascension` | `unique_id` **unchanged** at `_mount_ra`. Still HOURS and still the MOUNT's epoch, JNOW on this rig (§3.7) — feeding it back into a slew is wrong twice, a 15× unit error and a precession error. |
| `sensor.n_i_n_a_astrophotography_mount_sidereal_time` | `sensor.<instance>_mount_sidereal_time` | `unique_id` **unchanged**. Stays `DIAGNOSTIC`. |
| `sensor.n_i_n_a_astrophotography_safety_monitor_name` | removed | Device metadata. Its `unique_id` was `_safetymonitor_name`; the monitor's own entities are `binary_sensor.<instance>_safety_monitor_unsafe` and `binary_sensor.<instance>_safety_monitor_connected` (C1). |
| `sensor.n_i_n_a_astrophotography_sequence_progress` | `sensor.<instance>_sequence_progress` | `unique_id` **unchanged**. 1.4.5 read `ProgressExposures`, which lives on `/sequence/state` — 53 KB, not polled (§6.1) — and off the top level, so it too read nothing here. 2.0 reads the innermost node's own `Iterations` fraction, and `unknown` where no node counts: a Target Scheduler container keeps its target list internally and publishes no count. |
| `sensor.n_i_n_a_astrophotography_sequence_status` | removed | Node `Status` persists from the loaded sequence file and from prior runs, so an idle rig reports `RUNNING` with nothing happening (§6.2). `binary_sensor.<instance>_sequence_running` is the survivor, from the activity heuristic. |
| `sensor.n_i_n_a_astrophotography_sequence_target` | `sensor.<instance>_sequence_target` | `unique_id` **unchanged** at `_sequence_target_name`. 1.4.5 read `TargetName` off the top level of `/sequence/json`, which answers a LIST — so it read nothing on this rig. 2.0 takes the newest `TS-TARGETSTART`, falling back to the innermost `TargetName` in the tree for a sequence that is not Target-Scheduler-driven. |
| `sensor.n_i_n_a_astrophotography_time_to_meridian_flip` | `sensor.<instance>_mount_time_to_meridian_flip` | `unique_id` **unchanged** at `_mount_time_to_meridian_flip`. Still MINUTES, so `meridian_flip_warning.yaml`'s threshold keeps its unit; it now carries `device_class: duration`, so Home Assistant will convert on display. The 24-hour untracked sentinel reads `unknown`; **12 h does not** — a mount inside a pier-side window reports it legitimately (§11). |
| — | `sensor.<instance>_mount_side_of_pier` | **New.** `DIAGNOSTIC`. The meridian-flip maths needs it (§11), and it is the one field that says whether a flip has already happened. |
| — | `sensor.<instance>_flat_panel_cover_state` | **New.** `DIAGNOSTIC`. `CoverState` is `Open | Closed | NeitherOpenNorClosed | Unknown | Error`; `switch.<instance>_flat_panel_cover` cannot express a cover stuck between the two. |
| — | `sensor.<instance>_dome_shutter_status` | **New.** `DIAGNOSTIC`, disabled by default, `verified: False`, and created only on a rig that reports a dome. The survivor of `binary_sensor.dome_shutter_open`, whose `== 0` threw four of `ShutterStatus`'s five values away. |

## Entities — the switch device's channels (C8)

No 1.4.5 row: 1.4.5 read `/equipment/switch/info` for nothing but the device
name. Each channel of the N.I.N.A. switch device becomes **one** entity, on the
platform its shape names (§5.3.5), all on the `"<instance> Switch"` device and
all **enabled by default**:

| Channel shape | Platform | Entity id |
|---|---|---|
| `ReadonlySwitches` — no range at all | `sensor` | `sensor.<instance>_switch_<channel name>` |
| writable, `Maximum − Minimum == StepSize` | `switch` | `switch.<instance>_switch_<channel name>` |
| writable, a wider range | `number` | `number.<instance>_switch_<channel name>` |

`unique_id` is `{entry_id}_switch_channel_{Id}` on every one, keyed on the
channel's own `Id` rather than its position, so a channel the driver adds later
does not renumber the entities after it. A channel the driver gives no `Id` at
all falls back to a synthesized index, offset per list so a writable and a
read-only channel cannot collide on one key.

A channel none of the three shapes claims — writable, but reporting no usable
range — gets **no entity**, and says so in the log once. A channel whose range
is `Min 0 / Max 0 / Step 0` is not binary: that is what a disconnected device
reports, and a switch built from it would have the same value for on and off.
A channel the driver stops reporting reads `unavailable`, not `unknown` — the
device is still connected, and a command for an index that no longer exists is
answered `Success: true`. A channel the driver does not name
takes `Channel {Id}`: an empty entity name resolves to the device's own under
`has_entity_name`, which would collapse every unnamed channel onto one id.

The `number` and `sensor` halves ship without a unit or a device class —
`WritableSwitches` and `ReadonlySwitches` carry neither, and inferring volts
from a channel called "voltage" would mislabel every channel the guess is wrong
about.

On this rig the switch device is a Home Assistant bridge exposing two mains
outlets, so its entities duplicate ones that already exist natively. That is
accepted: the same device through two integrations is ordinary, and the case
the default is written for is a real ASCOM switch — a Pegasus Powerbox with its
outlets, dew heaters and gauges.

## Entities — `event` and the flat wizard (C9)

No 1.4.5 rows: 1.4.5 surfaced neither.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| — | `event.<instance>_error` | **New** (§5.4). An `event` entity, not a `sensor`: a plate solve that failed twenty minutes ago is not a condition the rig is still in. Event types `platesolve_failed`, `camera_download_timeout` and `autofocus_timeout`. `DIAGNOSTIC`. **Best-effort and solver-specific** — the `ERROR-*` events are log-file regex scrapes, `ERROR-PLATESOLVE` matches ASTAP only, and `ERROR-AF` appears dead in the plugin, which is why the autofocus arm is the fold's own timeout verdict instead. `CAMERA-DOWNLOAD-TIMEOUT` is named from the plugin's source and has never appeared in a capture. |
| — | `sensor.<instance>_flats_state` | **New.** `DIAGNOSTIC` and **disabled by default** (§5.3.3). |
| — | `sensor.<instance>_flats_total_iterations` | **New.** As above. The idle wizard's `-1` reads `unknown`. |
| — | `sensor.<instance>_flats_completed_iterations` | **New.** As above. |

`/flats/status` observes only flats started **through the API**. This rig runs
Target Scheduler Flats, so it reads `{State: "Finished", TotalIterations: -1,
CompletedIterations: -1}` straight through a completed dawn run — confirmed —
and an entity reporting a stale `Finished` all night is worse than no entity.
Enable them on a rig that starts its flats through the API.

## Configuration

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `poll_interval` 5–300 s, in both the add and the options form | 5–60 s, in both | **Breaking.** The tiering design (§6.1) assumes a fast tier near 10 s; a 300 s poll defeats it. A value above 60 already stored on an entry keeps polling at that rate — nothing rewrites it — and has to be lowered to 60 or less the next time the options form is submitted. |
| entry title `N.I.N.A. <version> @ <host>` | entry title is the instance name | The version in a title goes stale on the rig's next update, and the title is what names the hub device. |
| — | entry data `instance_name`, required in the add form, default `N.I.N.A.` | Multi-rig (§5.1): the hub device is named `"<instance>"` and every equipment device `"<instance> <Kind>"`. An entry created before 2.0 carries no `instance_name` and falls back to its entry title. A blank name is refused — it would title the entry `""` and name devices `" Camera"` — and so is one another entry already uses, which would name two rigs' devices alike. |
| entry data `api_version`, a `v2`-only dropdown | the dropdown is gone | It only ever offered one value. The key is still read from existing entry data by the legacy client that serves the unmigrated services; phase C retires both. |
| — | options `rollover_hour`, 0–23, default 12 | Where the session boundary falls (§4.4). Noon local is the astronomical night's natural break; a rig west of the meridian on UTC needs to move it. |
| — | the hub device carries a `configuration_url` of `http://<host>:<port>` | The rig's own web UI, one click from the device page. |

## Actions (D1)

Three actions never worked in 1.4.5: each sent a parameter the API does not
bind, and the API answers `Success: true` to a parameter it did not recognise,
so nothing at the call site could tell.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `camera_capture` field `binning` | removed | It bound nothing on the wire — `/equipment/camera/capture` takes no binning — and a parameter that looks like it works is worse than none. |
| `camera_capture` field `exposure` | `duration` | The API reads `duration`, so the exposure length was ignored and defaulted. |
| `camera_capture` field `filter_index` | removed | As `binning`. |
| `mount_slew` fields `ra` (hours), `dec` | `ra_degrees`, `dec_degrees`, both **J2000** | The endpoint reads degrees; 1.4.5 converted from hours, which reads plausibly for a coordinate that came back out of `MountInfo` — reported in the MOUNT's epoch and in hours, so feeding one back was wrong twice. |
| `sequence_load` field `path` | `sequence_name` | The API reads `sequenceName`, and it is the name N.I.N.A. lists, not a path. |
| — | every action takes an optional `device_id` target | 1.4.5 resolved "the first configured entry", so a second rig was unreachable from every action however it was targeted. Untargeted still works with one instance loaded; with two it is refused rather than guessed at. |

Actions now register at startup rather than per config entry (Bronze
`action-setup`), so an automation referencing one validates while the entry is
unloaded and fails legibly at call time instead.

## Blueprints (D2)

All five were rewritten, and their inputs renamed: an automation built from one
has to be rebuilt. Every one hardcoded entity ids this release renames or
removes, so all five were inert on 2.0 either way.

| 1.4.5 | 2.0 | Why |
|---|---|---|
| `guiding_alert.yaml` hardcoded `sensor.guider_rms_total`, `binary_sensor.guider_is_guiding` | `rms_sensor`, `guider_entity`, `nina_rig` inputs | Every 2.0 id carries the instance name, so a hardcoded one is wrong on every install but the author's. |
| `meridian_flip_warning.yaml` triggered on `below: 10` minutes | triggers on `MOUNT-BEFORE-FLIP`, plus a threshold computed from `flip_fires_at_minutes` | The flip fires when the reading reaches `(Max − Min)` — 10 minutes on this rig — so `below: 10` warned *at* the flip, and both bounds are per-profile. |
| `session_startup.yaml` input `sequence_path` | `sequence_name` | Follows the action. |
| `weather_abort.yaml` triggered on wind, rain and cloud thresholds | those triggers are gone; weather belongs to the optional resume conditions | Weather is telemetry, not an abort authority: a forecast-backed source reads 0% cloud while you sit under a cloud. |
| `weather_abort.yaml` trigger `binary_sensor.observatory_safe` | `safety_entity` (`to: "on"`) **and** `safety_connected_entity` (`to: "off"` for 30 s), as two triggers | The `SAFETY` device class is on = problem, and the monitor dropping out is its own abort reason — one a merged `off/unavailable/unknown` set cannot express, since `unavailable` fires on every Home Assistant restart. |

## Cards (D3)

| 1.4.5 | 2.0 | Why |
|---|---|---|
| bare entity ids, e.g. `sensor.mount_ra` | `prefix:` config, e.g. `sensor.${prefix}_mount_right_ascension` | 2.0 ids carry the slugified instance name. `prefix` defaults to `n_i_n_a`. |
| `nina-observatory-card` buttons called actions untargeted | optional `device_id:` config | An untargeted action is refused once a second rig is configured. |
