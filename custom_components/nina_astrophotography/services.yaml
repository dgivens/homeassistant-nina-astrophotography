camera_cool:
  name: Cool Camera
  description: Start cooling the camera sensor to a target temperature.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography
    temperature:
      name: Target Temperature (°C)
      description: Target sensor temperature in degrees Celsius (e.g. -10).
      required: true
      example: -10
      selector:
        number:
          min: -30
          max: 30
          unit_of_measurement: "°C"
    minutes:
      name: Ramp Time
      description: >-
        Minutes over which to ramp to the target temperature. Omit to use the
        ramp N.I.N.A.'s own profile specifies for this camera.
      example: 10
      selector:
        number:
          min: 1
          max: 60
          unit_of_measurement: min

camera_warm:
  name: Warm Camera
  description: Gradually warm the camera sensor back to ambient temperature.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography
    minutes:
      name: Ramp Time
      description: >-
        Minutes over which to ramp back to ambient. Omit to use the ramp
        N.I.N.A.'s own profile specifies for this camera.
      example: 10
      selector:
        number:
          min: 1
          max: 60
          unit_of_measurement: min

camera_capture:
  name: Capture Image
  description: Take a single camera exposure.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography
    duration:
      name: Exposure
      description: Exposure duration in seconds.
      required: true
      example: 120
      selector:
        number:
          min: 0.001
          max: 3600
          unit_of_measurement: s
          mode: box
    gain:
      name: Gain
      description: Camera gain override (optional).
      example: 100
      selector:
        number:
          min: 0
          max: 5000
          mode: box
    save:
      name: Save to Disk
      description: Whether to save the captured image to disk.
      default: false
      selector:
        boolean:

camera_abort_capture:
  name: Abort Capture
  description: Abort the current camera exposure immediately.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

mount_slew:
  name: Slew Mount
  description: Slew the telescope mount to J2000 RA/Dec coordinates.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography
    ra_degrees:
      name: Right Ascension
      description: Target J2000 RA in decimal degrees (0–360).
      required: true
      example: 83.822
      selector:
        number:
          min: 0
          max: 360
          step: "any"
          unit_of_measurement: "°"
          mode: box
    dec_degrees:
      name: Declination
      description: Target J2000 Dec in decimal degrees (−90 to +90).
      required: true
      example: -5.3911
      selector:
        number:
          min: -90
          max: 90
          step: "any"
          unit_of_measurement: "°"
          mode: box

mount_park:
  name: Park Mount
  description: Send the telescope mount to its park position.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

mount_unpark:
  name: Unpark Mount
  description: Unpark the telescope mount.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

mount_set_tracking:
  name: Set Tracking
  description: Enable or disable sidereal tracking.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography
    enabled:
      name: Tracking Enabled
      required: true
      selector:
        boolean:

focuser_move:
  name: Move Focuser
  description: Move the focuser to an absolute step position.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography
    position:
      name: Position
      description: Absolute focuser position in steps.
      required: true
      example: 25000
      selector:
        number:
          min: 0
          max: 200000
          mode: box
          unit_of_measurement: steps

focuser_auto_focus:
  name: Auto Focus
  description: Trigger an autofocus routine using the configured method.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

filterwheel_change_filter:
  name: Change Filter
  description: Rotate the filter wheel to the specified filter slot.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography
    filter_index:
      name: Filter Index
      description: Zero-based index of the target filter slot.
      required: true
      example: 0
      selector:
        number:
          min: 0
          max: 20

guider_start:
  name: Start Guiding
  description: Start the autoguider.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography
    force_calibration:
      name: Force Calibration
      description: Force a new calibration run before guiding begins.
      default: false
      selector:
        boolean:

guider_stop:
  name: Stop Guiding
  description: Stop the autoguider.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

dome_open:
  name: Open Dome
  description: Open the dome shutter.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

dome_close:
  name: Close Dome
  description: Close the dome shutter.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

dome_park:
  name: Park Dome
  description: Park the dome.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

sequence_start:
  name: Start Sequence
  description: Start the currently loaded imaging sequence.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

sequence_stop:
  name: Stop Sequence
  description: Stop the currently running sequence.
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography

sequence_load:
  name: Load Sequence
  description: Load a sequence by name (does not start it).
  fields:
    device_id:
      name: Rig
      description: >-
        Which N.I.N.A. instance to act on. Optional while one instance is
        configured, and required once a second is.
      selector:
        device:
          integration: nina_astrophotography
    sequence_name:
      name: Sequence Name
      description: >-
        The sequence's name as N.I.N.A. lists it, without the .ninasequence
        extension. Not a path.
      required: true
      example: "M42"
      selector:
        text:
